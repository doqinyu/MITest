    /**
     * 十个文件  0-9 ，需要把10个文件变成1个，混合后顺序
     * 0 号文件的第一行写入新文件的第一行
     * 1 号文件的第一行写入新文件的第二行
     * ...
     * 每个文件的行数不固定
     * 文件行数不固定
     *
     * 多线程的方式读写
     * 输出新文件的内容
     *
     * @param args
     */