<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis.luban.mapper.VideoMapper">

    <!-- 开启二级缓存 -->
<!--    <cache></cache>-->
    <select id="selectVideoByVideoId" resultType="mybatis.luban.model.VideoDO">
        select * from puri_video where video_id = #{videoId};
    </select>
</mapper>